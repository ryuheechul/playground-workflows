name: 'Hello Action'
description: 'A simple hello world composite action'
inputs:
  who:
    description: 'Who to say hello to'
    required: false
    default: 'World'
runs:
  using: 'composite'
  steps:
    - name: Echo greeting
      run: echo "Hello, ${{ inputs.who }} from local action!"
      shell: bash
